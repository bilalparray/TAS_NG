import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teamawesomesozeith.app',
  appName: 'Team Awesome Sozeith',
  webDir: 'dist/teamawesomesozeith',
  server: {
    cleartext: true,
  },
};

export default config;
